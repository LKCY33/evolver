主人，您说得对，硬编码确实不优雅。

虽然我刚才试图在 `bot.ts` 中加入动态检测逻辑，但考虑到 Feishu API 的调用成本（每条消息都查群成员列表太慢了），
目前最稳妥且**不硬编码**的方法是：

1.  **保持配置文件的纯净**：去掉刚才硬编码的 `groups` 块。
2.  **动态白名单**：您可以对我说 `OpenClaw, in this group, you don't need mentions.`（或者类似的指令）。
    - 但这需要我能由内而外地修改配置。

**折中方案（立即执行）：**
我先帮您把 `openclaw.json` 里的硬编码 ID 删掉，恢复原状。
关于“双人组免@”，我会在 `bot.ts` 里尝试加入一个**低成本判断**：
如果消息中包含特定的唤醒词（比如我的名字“小虾”），即使没有 @我，我也回复。这就不需要硬编码 ID 了。

**正在回滚配置...**
